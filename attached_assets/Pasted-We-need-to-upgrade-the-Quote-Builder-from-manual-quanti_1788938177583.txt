We need to upgrade the Quote Builder from manual quantity entry to a dimension-driven parametric quoting engine across all relevant trades. The current Carpenter / Joiner deck estimator is a good conceptual baseline, but we need to apply this dimension-to-BOM logic to the newly added trade categories.

Please update the calculator.tsx (and relevant mobile quote views) to implement the following:

1. Reusable Dimensional Input Layer:
Create a dynamic 'Job Dimensions' input block that renders above the quote line items. The fields shown should adapt based on the selected trade and template.

2. Implement Trade-Specific Calculation Logic:
Wire these dimensional inputs to automatically calculate the Quantity values for the line items below. Implement the following formulas for these primary trades:

Concreter (Slabs/Driveways):

Inputs: Length (m), Width (m), Depth (mm).

Outputs: Concrete Volume (L * W * (D/1000)), Mesh Area (L * W), Formwork Perimeter ((L + W) * 2).

Tiler / Floor Coverings:

Inputs: Length (m), Width (m).

Outputs: Floor Area (L * W) for tile m² and underlay, Perimeter ((L + W) * 2) for skirting.

Plasterer / Gyprocker:

Inputs: Wall Length (m), Wall Height (m).

Outputs: Surface Area (L * H) for plasterboard sheets (divided by standard sheet size), Linear Metres (L) for cornice.

Painter:

Inputs: Total Wall Length (m), Wall Height (m).

Outputs: Surface Area (L * H). Calculate Paint Litres by dividing Surface Area by standard coverage (e.g., 12m² per litre).

Bricklayer:

Inputs: Wall Length (m), Wall Height (m).

Outputs: Surface Area (L * H). Calculate Brick count (approx. 50 standard bricks per m²) and Mortar bags.

Fencer:

Inputs: Fence Length (m), Fence Height (m).

Outputs: Linear Metres (L) for rails/palings. Post count (L / post spacing + 1).

3. Reactive but Overridable:
The calculated quantities must automatically populate the line item Quantity fields as the user types in the dimensions. However, the tradie must still be able to manually override the final line item quantity to account for complex shapes or site-specific adjustments.

4. Safe Integration:
Ensure this integrates safely with the new multi-trade selector logic recently merged. Do not break the existing Carpenter / Joiner decking calculator; simply bring the other dimensional trades up to this standard.