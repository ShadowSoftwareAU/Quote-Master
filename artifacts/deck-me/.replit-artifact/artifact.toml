kind = "web"
previewPath = "/"
title = "Deck Me"
version = "1.0.0"
id = "artifacts/deck-me"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20226

[services.development]
run = "pnpm --filter @workspace/deck-me run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/deck-me", "run", "build" ]
publicDir = "artifacts/deck-me/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20226"
BASE_PATH = "/"
